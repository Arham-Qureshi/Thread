{
  "name": "thread",
  "version": "1.0.0",
  "description": "Universal, stateful context migration between any LLMs",
  "scripts": {
    "build:wasm": "make -C src/wasm_engine",
    "build": "npm run build:wasm && webpack --mode production",
    "build:all": "npm run build",
    "dev": "webpack --mode development --watch"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0"
  },
  "dependencies": {
    "cytoscape": "^3.34.0"
  }
}
